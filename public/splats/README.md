# Splats

Place your own splat files in this folder.

This repository does not commit personal splat datasets by default (they can be large and may be private).

See the main `README.md` section “Custom Assets” for how to select a splat at runtime.

